async function notifyIncidentAssigned(){
    try {
        
    } catch (error) {
        console.error('Failed to notify assignment', error)
        throw error
    }
}

async function notifyStatusChange() {
    try {
        
    } catch (error) {
        console.error('Failed to notify status change')
        throw error
    }
}